#include <iostream>
using namespace std;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int ab, bc, ca;
	cin >> ab >> bc >> ca;

	cout << ab * bc / 2 << "\n";

	return 0;
}
